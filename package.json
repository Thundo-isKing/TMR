{
  "name": "tmr-app",
  "version": "1.0.0",
  "description": "TMR - Time Replication App with Google Calendar, Push Notifications, and Todos",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "cd server && npm start",
    "build": "cd server && npm install && npm rebuild",
    "postinstall": "cd server && npm install && npm rebuild",
    "reset-password": "npm --prefix server run reset-password --"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "node-fetch": "^2.7.0",
    "web-push": "^3.5.0"
  }
}
